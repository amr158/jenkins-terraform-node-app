pipeline {
    agent any
    stages {
        stage('build infrastructure') {
            steps {
                withAWS(credentials: 'amr_gehad', region: 'eu-west-1') {
                    dir("terraform") { 
                        sh 'terraform init -reconfigure'
                        sh 'echo "yes" | terraform apply --var-file dev.tfvars -auto-approve'
                    }
                }
            }
        }
    }
}
